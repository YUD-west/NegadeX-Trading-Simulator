# Leave empty in development — Vite proxies /api and /socket.io to localhost:5000
# Set to your Railway backend URL in production, e.g.:
#   VITE_API_URL=https://negadex-api.up.railway.app
VITE_API_URL=
